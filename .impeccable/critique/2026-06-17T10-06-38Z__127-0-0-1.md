---
target: "http://127.0.0.1:5057/"
total_score: 31
p0_count: 0
p1_count: 1
timestamp: 2026-06-17T10-06-38Z
slug: 127-0-0-1
---
**Design Health Score**

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Strong once a job starts; before start there is no runtime estimate or system-readiness preview. |
| 2 | Match System / Real World | 3 | Labels are plain, but raw filesystem paths and "Metal active" still read technical. |
| 3 | User Control and Freedom | 4 | Pause, continue, and terminate are present and mapped to the job state. |
| 4 | Consistency and Standards | 4 | Controls, spacing, focus, radius, and button language are consistent. |
| 5 | Error Prevention | 2 | Result folder is a free text path with no browse/preset/validation affordance. |
| 6 | Recognition Rather Than Recall | 3 | The user must understand or remember folder paths and hint formatting. |
| 7 | Flexibility and Efficiency | 3 | Efficient for a single job; repeated daily use lacks saved folders/glossary presets. |
| 8 | Aesthetic and Minimalist Design | 4 | Quiet, task-first, restrained, and free of decorative AI patterns. |
| 9 | Error Recovery | 2 | Upload/start failures collapse into generic messages with little next-step guidance. |
| 10 | Help and Documentation | 3 | The UI has enough labels, but long-job expectations and persistence limits need inline support. |
| **Total** | | **31/40** | **Solid utility with operational-confidence gaps** |

**Anti-Patterns Verdict**

Does this look AI-generated? Not in the usual visual way. It avoids the obvious tells: no purple gradient, no glass panels, no fake hero, no decorative SVG, no nested card stack, no metric theater. It reads like a restrained local Mac utility, which fits the product register.

The remaining "AI smell" is not visual decoration. It is omission: the interface exposes the right controls but does not yet fully explain the operational risks around where files go, how long a large-v3 job may take, and what to do when something fails.

**Deterministic scan**: `detect.mjs` returned `[]` across `work/transcribe_app/templates/index.html`, `work/transcribe_app/static/styles.css`, and `work/transcribe_app/static/app.js`. No detector findings or false positives.

**Visual overlays**: Browser overlay injection was attempted on a fresh tab and failed because this Codex Browser Playwright surface is read-only for mutation. No user-visible overlay is available. Fallback evidence came from DOM snapshots, desktop and narrow-viewport screenshots, and computed layout reads.

**Overall Impression**

The app has the right product posture: quiet, direct, and operational. The biggest opportunity is not making it prettier; it is making it feel safer for a two-hour transcription job. The user should know where the transcript will land, roughly what commitment they are starting, and how to recover when the local model or path handling fails.

**What's Working**

- The first screen is the actual workflow: upload, language, output format, result folder, hints, start. No landing-page layer gets in the way.
- The visual system fits the product brief. Cool neutrals, one teal accent, 8px controls, no decorative shadows, and standard form controls make it feel reliable.
- The job panel structure is strong: progress, ETA, elapsed time, pause/continue/terminate, system pressure, save path, logs, and downloads are the right information set for long local work.

**Priority Issues**

**[P1] Result folder feels fragile**

Why it matters: The result folder is the one thing users must trust after a long run. The current field is a raw, clipped filesystem path, especially on desktop in the three-column grid and on narrow viewports. A user can easily miss the final destination or type a path that behaves differently than expected.

Fix: Replace the single raw text field with a compact location control: presets for "Default", "Downloads", and "Documents", plus an advanced custom path field. Show the resolved folder below the control in wrapped text. Validate the folder before starting and add an "Open result folder" action when a job finishes.

Suggested command: `$impeccable harden`

**[P2] No preflight time expectation before starting**

Why it matters: This product is for long transcription jobs, and large-v3 can be slow on this Mac. Users need to know whether they are starting a 10-minute task or a multi-hour task before they commit.

Fix: After file selection, show file name, size, detected duration, and a conservative estimated runtime range before submit. Keep it honest by labeling it as an estimate. Reuse the existing ETA logic once the job starts.

Suggested command: `$impeccable onboard`

**[P2] Pause/continue/terminate are strong, but their boundaries are unclear**

Why it matters: The controls imply durable workflow control. In reality, pause/continue controls the native transcription process, while service restarts or terminated jobs are not resumable from the same point. That gap matters for two-hour files.

Fix: Add concise state copy in the job panel: "Pauses the local model process on this Mac" and, on cancelled/failed jobs, make retry behavior explicit. If restart recovery is not supported, say so in the failed-state recovery UI.

Suggested command: `$impeccable clarify`

**[P2] Error recovery is too generic**

Why it matters: A failed upload, invalid output location, missing model, or terminated job needs a different next action. Generic "Upload failed" creates anxiety and pushes the user back to terminal/log thinking.

Fix: Add field-level errors for output folder and upload validation; preserve the selected file/settings after recoverable failures; add retry and "use default folder" actions where possible.

Suggested command: `$impeccable harden`

**[P3] Terminology hints are useful but under-shaped for repeated use**

Why it matters: Cantonese transcription quality often depends on recurring names, product terms, acronyms, and mixed English phrases. A placeholder alone makes the user rediscover the same hint style each job.

Fix: Keep the textarea, but add a lightweight saved glossary list later: recent hints, pinned terms, and a small "add from corrected transcript" path. This also connects cleanly to the lightweight learning direction.

Suggested command: `$impeccable shape`

**Persona Red Flags**

**First-time Mac owner**: They can start a job, but the default Application Support folder is not a place most non-technical users recognize. The result folder is clipped, and there is no "Choose" or preset path. They may finish a long transcript and not know where it went.

**Long-job operator**: They need to decide whether the Mac can be left alone. Before selecting Start, the UI does not show file duration, expected runtime, or system pressure. During a failure, it does not clearly separate retryable path/upload failures from model/process failures.

**Future maintainer / power user**: The UI is compact and clear, but repeated workflows will become tedious: output locations, terminology hints, and post-run folder opening are still manual. The screen does not expose enough durable workflow affordances for daily use.

**Minor Observations**

- Rename "Output" to "Output format" to match the user's mental model.
- "Start transcript" should become "Start transcription" for cleaner action wording.
- "GPU: Metal active" is honest, but it should say "Metal backend active" or "Metal enabled" so it is not mistaken for measured GPU usage.
- The progress region should expose job status changes through an `aria-live` region that includes the job title/status, not only the metric row.
- The narrow viewport layout is stable and readable; no text overlap was observed.

**Questions to Consider**

- What should be the default save behavior for a non-technical user: Application Support, Downloads, or Documents?
- Should the app optimize the first screen for "start fast" or "start confidently" when the file may run for hours?
- Should pause/continue be presented as technical process control, or as a friendlier job control with clearer limits?
